const Header = () => (
  <nav className="navbar bg-light px-4">
    <span className="navbar-brand mb-0 h1">Payroll Dashboard</span>
    <span className="text-muted">Welcome, Admin</span>
  </nav>
);

export default Header;